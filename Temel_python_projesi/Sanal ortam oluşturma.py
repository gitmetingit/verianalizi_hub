"""
     conda create -n maf python=3 (maf adında bir sanal ortam oluştur)
     conda env list (sanal ortamları listele)
     conda activate maf (maf sanal ortamına gir)
     conda list(indirilmiş paketleri listele )
     conda install numpy pandas=3.0 (numpy en güncel sürümü ve pandas'ın ilgili sürümünü yükle)
     conda upgrade pandas(pandas'ı güncelle)
     conda remove numpy(numpy kaldır)
     conda install seaborn matplotlib(seaborn ve matplotlib en güncel sürümleri yükle)
     conda env export > environment.yaml (sistem gereksinimleri enviroment.yaml dosyası oluştur.)
     conda deactivate (maf sanal ortamından çık)
     conda remove -n maf (maf sanal ortamını sil)

                                                                                """